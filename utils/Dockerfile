FROM alpine:3
LABEL maintainer="Mitz Amano <mitz@linux.com>"

# hadolint ignore=DL3018
RUN apk add --no-cache \
    bash \
    bind-tools \
    curl \
    gawk \
    ipmitool \
    jq \
    openssh \
    parallel \
    rsync
